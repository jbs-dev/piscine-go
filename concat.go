package piscine

// Concat concatenates two strings and returns the result
func Concat(str1 string, str2 string) string {
	return str1 + str2 // concatenates the two input strings using the '+' operator and returns the result
}
